'use strict';

angular.module('SevenUp')
    .factory('LexicalScanner', function (programCode) {
        result.GetTokens = function (programCode) {
            LexicalScanner.GetTokens(programCode);
        }

        return result;
    });
